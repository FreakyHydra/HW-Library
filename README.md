# HW-Library

> Legacy repository.

`HW-Library` is the historical predecessor of the current Orbis application.

The canonical home for current development is:

- `HowlingWhispers/HW-Orbis`
- branch: `main`

Do not start new Orbis features, fixes, migrations, or UI work in this repository.

This repository is retained only as historical reference for older Library-era implementation and branches. If something here is still useful, compare it against current `HW-Orbis` before carrying anything forward.
